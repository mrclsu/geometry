# geometry_ssh_hostname - show user@hostname, but only in an ssh session
# To always show user@hostname, use geometry_hostname instead

[[ -z $SSH_CLIENT && -z $SSH_CONNECTION && -z $SSH_TTY ]] && return
geometry_hostname
